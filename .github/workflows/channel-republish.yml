# Republish a package channel for a version that is already released.
#
# The other half of the bargain `release.yml` makes by running the managed
# channels in parallel, behind nothing and gating nothing: a Homebrew, Scoop or
# Chocolatey publish that fails must not strand a release, so there has to be a
# way to finish it afterwards without re-running one.
#
# `scripts/release-detect.ts` cannot help here for the same reason it cannot
# help the container image. Its states are `fresh`, `resume-npm` and
# `resume-github`; it knows nothing about a tap, a bucket or a community feed,
# so once npm and the tag are done a re-run of `release.yml` resolves to
# `complete` and skips these jobs entirely.
#
# Everything a descriptor contains — the version, the bundle URL and its sha256
# — is read from `release-manifest.json` attached to the published GitHub
# Release, never from a checkout. Release assets outlive workflow artefacts,
# which is what lets this run months after the release it repairs, and reading
# them rather than rebuilding means a repair cannot publish a descriptor
# pointing at bytes the release never had.
#
# The publish jobs then check out the commit that release was built from, so the
# templates rendering those values are that release's own. Together the two
# pin both halves: the manifest pins what a descriptor says, the commit pins the
# code that says it. Without the second, editing a formula template silently
# rewrites every older release anyone later repairs.
#
# The cost is that this file — always the default branch's, because that is
# where `workflow_dispatch` reads it from — invokes scripts from an older
# checkout. Adding a flag is safe, since the argument parsers ignore ones they
# do not know. Removing or renaming a flag that an older script requires is not,
# and would surface as a repair failing on an old version long after the change.
#
# What it will not do by default is overwrite. A channel already carrying this
# version with these exact bytes is left alone; one carrying a newer version is
# refused outright, because publishing over it downgrades everyone who installs
# from that channel. `force` exists for the narrow case this whole workflow is
# for — a descriptor published wrong rather than not at all — and even then it
# cannot move a channel backwards.
name: Republish a package channel

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'A released version, without the leading v, as MAJOR.MINOR.PATCH'
        required: true
        type: string
      channel:
        description: 'Which channel to republish'
        required: true
        type: choice
        options: [homebrew, scoop, chocolatey, winget]
      force:
        description: 'Replace this version in place. Only for a descriptor that was published wrong.'
        required: false
        default: false
        type: boolean

permissions:
  contents: read

env:
  NPM_CONFIG_FETCH_RETRIES: '3'
  NPM_CONFIG_FETCH_RETRY_MINTIMEOUT: '10000'
  NPM_CONFIG_FETCH_RETRY_MAXTIMEOUT: '60000'

# The same group `release.yml` and `container-republish.yml` use, so a repair
# cannot race a release. Never cancelled: this writes to other people's
# repositories and to a moderated feed, and being interrupted partway is the
# state it exists to fix.
concurrency:
  group: release
  cancel-in-progress: false

jobs:
  # ---------------------------------------------------------------------------
  # What does that release actually say it is?
  # ---------------------------------------------------------------------------
  prepare:
    name: Read the release
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      version: ${{ steps.inputs.outputs.version }}
      bundle: ${{ steps.inputs.outputs.bundle }}
      url: ${{ steps.inputs.outputs.url }}
      sha256: ${{ steps.inputs.outputs.sha256 }}
      force: ${{ steps.force.outputs.force }}
      templates: ${{ steps.templates.outputs.ref }}
      win_binary: ${{ steps.inputs.outputs.win_binary }}
      win_binary_url: ${{ steps.inputs.outputs.win_binary_url }}
      win_binary_sha256: ${{ steps.inputs.outputs.win_binary_sha256 }}
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      # Normalises the force flag to a string output, for the same reason
      # `release-pr.yml` normalises its dry-run flag: no workflow expression can
      # gate on a boolean input directly with confidence.
      #
      # `inputs.force && '--force' || ''` — what the two push jobs used to
      # interpolate — is wrong. A boolean input arrives as the *string* "false"
      # in some contexts, and a non-empty string is truthy, so it expanded to
      # `--force` whether or not the box was ticked. Leaving the box unticked
      # did nothing, and every repair overwrote in place.
      #
      # That is the one power this workflow is careful about. Force cannot move
      # a channel backwards and same-bytes is still a no-op, so the only thing
      # it changes is the case the refusal exists for: same version, different
      # bytes, nobody having asked to replace them.
      #
      # Anything that is not exactly "true" is treated as no force: an
      # unexpected value must not overwrite a published descriptor.
      - name: Resolve the force flag
        id: force
        env:
          REQUESTED: ${{ inputs.force }}
        run: |
          set -euo pipefail
          if [ "${REQUESTED}" = "true" ]; then force=true; else force=false; fi
          echo "force=${force}" >> "$GITHUB_OUTPUT"
          echo "Force: ${force}"

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .nvmrc

      # A draft is not a release: its assets are not anonymously downloadable,
      # so a descriptor pointing at one would install for nobody.
      - name: Refuse anything that is not a published release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          state=$(gh release view "v${VERSION}" --repo "${GITHUB_REPOSITORY}" --json isDraft,isPrerelease \
            --jq '"\(.isDraft) \(.isPrerelease)"')
          case "${state}" in
            'true '*)
              echo "::error::v${VERSION} is still a draft. Its assets are not downloadable."
              exit 1 ;;
            *' true')
              # `brew upgrade` and `scoop update` have no concept of a prerelease
              # channel, so publishing one makes it what everybody gets.
              echo "::error::v${VERSION} is a prerelease. The managed channels only ever carry stable releases."
              exit 1 ;;
          esac
          echo "v${VERSION} is a published stable release."

      - name: Download the release manifest
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ inputs.version }}
        run: gh release download "v${VERSION}" --repo "${GITHUB_REPOSITORY}" --pattern release-manifest.json --clobber

      - name: Read what that release publishes
        id: inputs
        run: node scripts/channel-inputs.ts --manifest release-manifest.json --repo "${GITHUB_REPOSITORY}"

      - name: Check the manifest describes the version that was asked for
        env:
          ASKED: ${{ inputs.version }}
          FOUND: ${{ steps.inputs.outputs.version }}
        run: |
          set -euo pipefail
          if [ "${ASKED}" != "${FOUND}" ]; then
            echo "::error::v${ASKED}'s manifest says it is ${FOUND}."
            exit 1
          fi

      # Which commit's templates render this release's descriptors.
      #
      # The version, URL and sha256 above pin what a descriptor *says*; they do
      # not pin the code that writes it. A repair runs from the default branch,
      # so without this a formula rebuilt a year later carries whatever
      # `package-manifests.ts` says then — a changed dependency, a changed
      # caveat, a changed install block — and publishes it as though that were
      # what the release shipped. Nobody reviewing the template change would
      # know they had also just changed four old releases.
      #
      # So the release's own commit wins. Re-rendering an old release with new
      # templates is a change to what that release publishes, and it belongs in
      # a reviewed code change rather than behind a dispatch checkbox.
      #
      # A manifest that records no commit is refused rather than rendered from
      # the default branch. That fallback was the one remaining way an
      # unverified ref could reach the credential-bearing jobs below, and it
      # produced exactly what the pin exists to prevent: an old release
      # re-rendered with today's templates, announced as a warning nobody reads.
      # It is unreachable today — `channel-inputs.ts` refuses a manifest with no
      # bundle, and manifests predating the commit field carry no bundle either —
      # so this closes a gap rather than removing a capability.
      #
      # The commit is *verified* rather than trusted. Until this check existed,
      # the only thing established about the manifest was that its version
      # matched the one asked for; its `commit` field then became the checkout
      # ref for jobs that hold the Docker, Chocolatey and tap credentials.
      # Resolving the release tag and requiring the two to agree is what ties
      # that ref to a release GitHub says exists, and the format check is what
      # stops anything that is not a commit id reaching `actions/checkout` at
      # all — a ref is not a SHA unless it looks like one.
      - name: Resolve which templates to render with
        id: templates
        env:
          GH_TOKEN: ${{ github.token }}
          COMMIT: ${{ steps.inputs.outputs.commit }}
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          if [ -z "${COMMIT}" ]; then
            echo "::error::v${VERSION}'s manifest records no commit, so the templates that rendered it cannot be identified."
            echo "::error::Refusing to republish from the default branch: that would change what this release publishes."
            exit 1
          fi

          case "${COMMIT}" in
            *[!0-9a-f]* | "")
              echo "::error::The manifest's commit '${COMMIT}' is not a lowercase hexadecimal SHA."
              exit 1 ;;
          esac
          if [ "${#COMMIT}" -ne 40 ]; then
            echo "::error::The manifest's commit '${COMMIT}' is ${#COMMIT} characters; a full SHA is 40."
            exit 1
          fi

          # `^{}` on an annotated tag dereferences it to the commit, so an
          # annotated and a lightweight tag compare equal. The tag is asked of
          # the API rather than of a checkout, because this job checks out the
          # default branch and a shallow clone of it need not contain the tag.
          tagged=$(gh api "repos/${GITHUB_REPOSITORY}/git/ref/tags/v${VERSION}" --jq '.object.sha')
          type=$(gh api "repos/${GITHUB_REPOSITORY}/git/ref/tags/v${VERSION}" --jq '.object.type')
          if [ "${type}" = "tag" ]; then
            tagged=$(gh api "repos/${GITHUB_REPOSITORY}/git/tags/${tagged}" --jq '.object.sha')
          fi

          if [ "${COMMIT}" != "${tagged}" ]; then
            echo "::error::v${VERSION}'s manifest says it was built from ${COMMIT}, but the tag points at ${tagged}."
            echo "::error::Refusing to check that out. The manifest and the release disagree about what this version is."
            exit 1
          fi

          echo "ref=${COMMIT}" >> "$GITHUB_OUTPUT"
          echo "Rendering with the templates from ${COMMIT}, which is what v${VERSION} is tagged at."

  # ---------------------------------------------------------------------------
  # Homebrew and Scoop: a file in a git repository, written through the contents
  # API so a concurrent push loses rather than this one.
  # ---------------------------------------------------------------------------
  push-formula:
    name: Republish the Homebrew formula
    needs: [prepare]
    if: inputs.channel == 'homebrew'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment: packaging
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          # The release's own templates, not the default branch's. See `prepare`.
          ref: ${{ needs.prepare.outputs.templates }}

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .nvmrc

      # Homebrew is installed on the ubuntu images but deliberately kept off PATH,
      # so `brew` is not a command here until this runs.
      - name: Put Linuxbrew on PATH
        run: |
          set -euo pipefail
          if [ ! -x /home/linuxbrew/.linuxbrew/bin/brew ]; then
            echo "::error::This runner image no longer ships Homebrew at the expected path."
            exit 1
          fi
          eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
          echo "${HOMEBREW_PREFIX}/bin" >> "$GITHUB_PATH"
          brew --version
          # The image ships a snapshot and nothing updates it. A stale one has
          # aborted mid-install with an error from Homebrew's own internals.
          brew update --quiet

      # `--online` fetches the URL and verifies the checksum. For a repair that
      # is the whole question: the release exists, so if the formula points at
      # something that is not there, this is where it is caught.
      - name: Audit the formula
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
          BUNDLE_URL: ${{ needs.prepare.outputs.url }}
          BUNDLE_SHA256: ${{ needs.prepare.outputs.sha256 }}
        run: >
          node scripts/audit-formula.ts --online
          --version "${VERSION}"
          --url "${BUNDLE_URL}"
          --sha256 "${BUNDLE_SHA256}"

      - name: Push the formula
        env:
          GH_TOKEN: ${{ secrets.TAP_BUCKET_TOKEN }}
          VERSION: ${{ needs.prepare.outputs.version }}
          BUNDLE_URL: ${{ needs.prepare.outputs.url }}
          BUNDLE_SHA256: ${{ needs.prepare.outputs.sha256 }}
          FORCE: ${{ needs.prepare.outputs.force }}
        run: |
          set -euo pipefail
          args=(--channel homebrew --repo looptroop-ai/homebrew-tap
            --version "${VERSION}" --url "${BUNDLE_URL}" --sha256 "${BUNDLE_SHA256}")
          if [ "${FORCE}" = 'true' ]; then args+=(--force); fi
          node scripts/channel-push.ts "${args[@]}"

  push-manifest:
    name: Republish the Scoop manifest
    needs: [prepare]
    if: inputs.channel == 'scoop'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: packaging
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          # The release's own templates, not the default branch's. See `prepare`.
          ref: ${{ needs.prepare.outputs.templates }}

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .nvmrc

      - name: Push the manifest
        env:
          GH_TOKEN: ${{ secrets.TAP_BUCKET_TOKEN }}
          VERSION: ${{ needs.prepare.outputs.version }}
          BUNDLE_URL: ${{ needs.prepare.outputs.url }}
          BUNDLE_SHA256: ${{ needs.prepare.outputs.sha256 }}
          FORCE: ${{ needs.prepare.outputs.force }}
        run: |
          set -euo pipefail
          args=(--channel scoop --repo looptroop-ai/scoop-bucket
            --version "${VERSION}" --url "${BUNDLE_URL}" --sha256 "${BUNDLE_SHA256}")
          if [ "${FORCE}" = 'true' ]; then args+=(--force); fi
          node scripts/channel-push.ts "${args[@]}"

  # ---------------------------------------------------------------------------
  # Chocolatey, whose recovery states are its own.
  # ---------------------------------------------------------------------------
  # A version on the community feed is immutable once accepted, and a submission
  # sits in moderation for as long as it sits there. So this job does one of two
  # different things depending on what is already true, and neither is "push
  # over the last one":
  #
  #   - the version is not on the feed: pack, install locally, submit
  #   - the version is on the feed: install it *from the feed* and report
  #
  # The second is what to run once moderation clears. It is the only check that
  # what a user gets from Chocolatey is what this release meant to publish, and
  # nothing in `release.yml` can perform it, because at release time the package
  # has not been reviewed yet.
  chocolatey:
    name: Republish or verify the Chocolatey package
    needs: [prepare]
    if: inputs.channel == 'chocolatey'
    runs-on: windows-latest
    timeout-minutes: 40
    environment: packaging
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          # The release's own templates, not the default branch's. See `prepare`.
          ref: ${{ needs.prepare.outputs.templates }}

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .nvmrc

      - name: Is this version on the feed already?
        id: feed
        shell: bash
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          set -euo pipefail
          # `choco search`, not `choco list`: Chocolatey v2 made `list` local-only.
          # It reports only approved packages, which is exactly the distinction
          # that matters — a submission still in moderation is not installable.
          #
          # The exit status is read separately from the output, because they
          # answer different questions and a pipeline inside `if` hides the
          # first: `set -e` does not fire in a condition, so a search that
          # *failed* — an unreachable source, a proxy or TLS error, a broken
          # client — printed nothing, read as "not on the feed", and sent this
          # job on to repack and resubmit a version that may well be published.
          # Chocolatey exits 0 for a search that found nothing and sets 2 only
          # under `useEnhancedExitCodes`, so anything else is an error and this
          # step stops on it rather than guessing.
          status=0
          found=$(choco search looptroop --exact --version "${VERSION}" --limit-output --source https://community.chocolatey.org/api/v2/) || status=$?
          if [ "${status}" -ne 0 ] && [ "${status}" -ne 2 ]; then
            echo "::error::choco search exited ${status}, so the feed could not say whether ${VERSION} is published."
            exit 1
          fi
          # Matched against the id, because `--exact` has matched by prefix on
          # some Chocolatey versions — the same comparison the published smoke
          # makes of the same output.
          if printf '%s\n' "${found}" | grep -qE '^looptroop\|'; then
            echo "published=true" >> "$GITHUB_OUTPUT"
            echo "looptroop ${VERSION} is approved and on the feed."
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
            echo "looptroop ${VERSION} is not on the feed (either never submitted, or still in moderation)."
          fi

      # By exact name, not `*-bundle.tar.gz`. The manifest names the asset this
      # release published, so asking for anything else means accepting whatever
      # a glob happens to match on a release that also carries an older or
      # differently-named archive.
      - name: Download the bundle this release published
        if: steps.feed.outputs.published == 'false'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.prepare.outputs.version }}
          BUNDLE: ${{ needs.prepare.outputs.bundle }}
        run: gh release download "v${VERSION}" --repo "${GITHUB_REPOSITORY}" --pattern "${BUNDLE}" --clobber

      # `--expect-sha256`, because these bytes were downloaded rather than built
      # here and go straight inside the package. Homebrew gets this check from
      # `brew audit --online`; Chocolatey has no equivalent, so it happens here
      # or not at all.
      - name: Pack and prove it locally
        if: steps.feed.outputs.published == 'false'
        shell: bash
        env:
          BUNDLE: ${{ needs.prepare.outputs.bundle }}
          BUNDLE_URL: ${{ needs.prepare.outputs.url }}
          VERSION: ${{ needs.prepare.outputs.version }}
          BUNDLE_SHA256: ${{ needs.prepare.outputs.sha256 }}
        run: |
          set -euo pipefail
          node scripts/build-choco.ts \
            --bundle "${BUNDLE}" \
            --url "${BUNDLE_URL}" \
            --version "${VERSION}" \
            --expect-sha256 "${BUNDLE_SHA256}" \
            --out dist-choco
          node scripts/smoke-choco.ts \
            --nupkg "dist-choco/looptroop.${VERSION}.nupkg" \
            --version "${VERSION}"

      - name: Submit it
        if: steps.feed.outputs.published == 'false'
        shell: bash
        env:
          CHOCOLATEY_API_KEY: ${{ secrets.CHOCOLATEY_API_KEY }}
          VERSION: ${{ needs.prepare.outputs.version }}
        run: >
          node scripts/choco-push.ts
          --nupkg "dist-choco/looptroop.${VERSION}.nupkg"
          --version "${VERSION}"

      # The only check that what Chocolatey serves is what this release meant to
      # publish. Everything before moderation proves the package we built; this
      # proves the package they approved.
      #
      # The same script that proved the package before submission, so the feed
      # is held to the same standard rather than a weaker one: it installs, runs
      # it, asserts `doctor` names chocolatey as the channel, uninstalls, and
      # confirms no shim is left behind. Checking only the version would pass a
      # package whose channel marker went missing in packaging — one that
      # installs and runs perfectly while telling every user to upgrade it with
      # the wrong command.
      - name: Install it from the feed and prove it
        if: steps.feed.outputs.published == 'true'
        shell: bash
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
        run: >
          node scripts/smoke-choco.ts --from-feed
          --version "${VERSION}"

  # ---------------------------------------------------------------------------
  # WinGet, whose recovery is a pull request rather than a write.
  # ---------------------------------------------------------------------------
  # The submission is retry-safe by construction — it reconciles with an open
  # pull request for this version rather than opening a second — so this job is
  # simply the same submission run again, for a release whose WinGet job failed
  # or was skipped for want of a credential.
  #
  # Nothing here can make Microsoft merge it. Like Chocolatey moderation, that
  # is a queue this workflow reports into and never waits on.
  # `ubuntu-latest`, like the Scoop job above: writing a Windows package
  # manager's descriptor needs no Windows, and this one clones `winget-pkgs`,
  # which is 689,147 files. On a Windows runner that shallow clone took 16
  # minutes of the 30 this job has; on Linux it takes under two.
  winget:
    name: Resubmit the WinGet manifests
    needs: [prepare]
    if: inputs.channel == 'winget'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: packaging
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          # The release's own templates, not the default branch's. See `prepare`.
          ref: ${{ needs.prepare.outputs.templates }}

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .nvmrc

      - name: Submit the manifests
        shell: bash
        env:
          WINGET_TOKEN: ${{ secrets.WINGET_TOKEN }}
          VERSION: ${{ needs.prepare.outputs.version }}
          WIN_BINARY: ${{ needs.prepare.outputs.win_binary }}
          WIN_BINARY_URL: ${{ needs.prepare.outputs.win_binary_url }}
          WIN_BINARY_SHA256: ${{ needs.prepare.outputs.win_binary_sha256 }}
        run: |
          set -euo pipefail
          if [ -z "${WINGET_TOKEN:-}" ]; then
            echo "::error::WINGET_TOKEN is not set in the packaging environment."
            exit 1
          fi
          if [ -z "${WIN_BINARY}" ]; then
            echo "::error::v${VERSION} carries no Windows binary; it predates the WinGet channel."
            exit 1
          fi
          node scripts/winget-submit.ts \
            --version "${VERSION}" \
            --url "${WIN_BINARY_URL}" \
            --sha256 "${WIN_BINARY_SHA256}"

  # ---------------------------------------------------------------------------
  # Verify the repair against the real feed.
  # ---------------------------------------------------------------------------
  #
  # Without this, a repaired channel is unverified until the next scheduled run
  # — which is up to a week of believing a fix worked because the push reported
  # success. Dispatched rather than called, for the same reasons as in
  # `release.yml`: a red smoke must not retroactively fail the repair that
  # succeeded, and it must be re-runnable on its own.
  #
  # `--only` narrows it to the channel that was repaired. A full matrix here
  # would fail unrelated latest-pointer assertions whenever the repair is for
  # anything other than the newest release.
  #
  # Chocolatey and WinGet are excluded on purpose: a push to either is a
  # submission, and the package is not publicly installable until moderation or
  # a manifest review completes — hours to days later.
  #
  # The smoke no longer reports that as a failure: those channels are
  # `moderated`, so a version still in its queue comes back as a deliberate
  # skip, timed from the submission rather than from the release. A dispatch
  # here would therefore be honest, and still useless — it would spend a Windows
  # runner to print what the push step already said. The channel is verified by
  # the weekly run once the queue moves.
  verify-repair:
    name: Verify the repaired channel
    needs: [prepare, push-formula, push-manifest]
    if: >-
      always()
      && (needs.push-formula.result == 'success' || needs.push-manifest.result == 'success')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      actions: write
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Dispatch the published install smoke
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.prepare.outputs.version }}
          CHANNEL: ${{ inputs.channel }}
        run: |
          set -euo pipefail
          gh workflow run published-smoke.yml \
            --repo "${GITHUB_REPOSITORY}" \
            --ref "v${VERSION}" \
            -f version="${VERSION}" \
            -f tier=weekly \
            -f only="${CHANNEL}" \
            -f report_mode=issue
          echo "Dispatched the published install smoke for ${CHANNEL} at v${VERSION}."
